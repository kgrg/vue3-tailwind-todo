# Repo Map — TaskFlow (justdo-vue)

> Auto-generated by `workflow-manager.prompt.md` (`action=create`) on 2026-03-01.

---

## Root

```
.
├── src/                         # Application source code
├── public/                      # Static assets served directly
├── document/framework/          # Internal framework documentation
├── docs/                        # Workflow kit documentation
├── artifacts/                   # Machine-readable repo facts
├── specs/                       # Feature specifications
├── .github/                     # GitHub config, agents, prompts
├── index.html                   # HTML entry point
├── package.json                 # Manifest (pnpm)
├── pnpm-lock.yaml               # Lockfile
├── vite.config.ts               # Vite build configuration
├── tailwind.config.ts           # Tailwind CSS configuration
├── tsconfig.json                # TypeScript config
├── tsconfig.node.json           # TypeScript config (Node)
├── cleanup.sh                   # Legacy cleanup script
└── README.md                    # Project README
```

---

## src/ — Application Source

### Entry

| File | Purpose |
|---|---|
| `src/main.ts` | App bootstrap: Vue 3 + Pinia + Router + Chart.js registration |
| `src/App.vue` | Root component (`<router-view>`) |

### Core (`src/core/`)

Shared infrastructure used across all modules.

| Directory | Purpose |
|---|---|
| `src/core/components/` | Base UI components: BaseCard, BaseInput, BaseCheckbox, BaseSelect, BaseTextarea, BaseTag, BaseListItem, ActivityStats, NavItem |
| `src/core/components/charts/` | Chart.js wrappers: ActivityCompletionChart, ActivityCategoryChart, ActivityHoursChart, ProductivityChart |
| `src/core/icons/` | Icon components + `index.js` exports |
| `src/core/composables/` | Shared composition functions |
| `src/core/utils/` | Utility functions |
| `src/core/accessibility/` | Accessibility utilities |
| `src/core/error-boundaries/` | Error handling components |
| `src/core/migrations/` | Data migration utilities |
| `src/core/performance/` | Performance optimization utilities |
| `src/core/persistence/` | Data persistence utilities |
| `src/core/repositories/` | Data access layer abstractions |
| `src/core/search/` | Search functionality |
| `src/core/styles/` | Core styling system |
| `src/core/themes/` | Theme system |
| `src/core/validation/` | Validation utilities |

### Feature Modules (`src/modules/`)

Each module is self-contained with `components/`, `store/`, `types/`, and optionally `pages/`.

| Module | Key Files | Pinia Store |
|---|---|---|
| `activities/` | `ActivityCard.vue`, `NewActivityModal.vue`, `activity.types.ts` | `useActivityStore` (`activities.store.ts`) |
| `auth/` | `LoginForm.vue`, `LoginPage.vue`, auth types (`index.ts`) | `useAuthStore` (`auth.store.ts`) |
| `habits/` | `HabitCard.vue`, habit types (`index.ts`) | `useHabitStore` (`habits.store.ts`) |
| `labels/` | Label components, store, types | (label store) |
| `tasks/` | `TaskItem.vue`, `TodoList.vue`, `todoStore.ts` | `useTodoStore` (`todoStore.ts`) |

### Layouts (`src/layouts/`)

| File | Purpose |
|---|---|
| `DashboardLayout.vue` | Main app layout (sidebar + content area) |
| `AuthLayout.vue` | Authentication pages layout |
| `AppSidebar.vue` | Sidebar navigation |
| `AppHeader.vue` | App header |
| `MainLayout.vue` | Primary layout |

### Pages (`src/pages/`)

| File | Route | Purpose |
|---|---|---|
| `DashboardView.vue` | `/` | Main dashboard with stats + charts |
| `TodayView.vue` | `/today` | Today's activities |
| `TodayPage.vue` | (unused) | Legacy duplicate of TodayView |
| `ActivityListView.vue` | `/activities`, `/upcoming`, `/categories/:cat`, `/tags/:tag`, `/completed`, `/settings` | Activity list with filters |

### Router (`src/router/`)

| File | Purpose |
|---|---|
| `index.ts` | Route definitions, layout-based routing, auth navigation guard |

### Legacy (`src/stores/`)

| File | Purpose |
|---|---|
| `todoStore.js` | Legacy todo store (plain JS, migration pending) |

### Styles (`src/styles/`)

| File | Purpose |
|---|---|
| `main.css` | Tailwind CSS entry point |

---

## public/

| Path | Purpose |
|---|---|
| `public/images/` | Habit images, logos |
| `public/vite.svg` | Vite logo |

---

## Workflow Kit (`.github/`, `docs/`, `artifacts/`)

| File | Purpose |
|---|---|
| `.github/copilot-instructions.md` | Copilot context instructions |
| `.github/agents/*.agent.md` | Role-specific agents (planner, implementer, reviewer, test-writer, release) |
| `.github/prompts/*.prompt.md` | Task-specific prompts |
| `docs/ARCH_SUMMARY.md` | Architecture summary |
| `docs/REPO_MAP.md` | This file |
| `docs/BUILD_MATRIX.md` | Build configuration |
| `docs/DEPENDENCY_GRAPH.md` | Dependency relationships |
| `docs/WORKFLOW_AUDIT_REPORT.md` | Audit findings |
| `artifacts/repo_facts.json` | Machine-readable repo facts |

---

## Specs

| Path | Purpose |
|---|---|
| `specs/001-make-labels-feature/` | Label feature specification |
